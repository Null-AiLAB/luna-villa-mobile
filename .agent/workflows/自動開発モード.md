---
description: 自動開発モード (開発サイクルの自動化)
---

// turbo-all

1. 現在のGitの状態を確認する
// turbo
```powershell
git status
```

2. ブランチの確認
// turbo
```powershell
git branch --show-current
```

3. コードの品質チェック (Lint)
// turbo
```powershell
npm run lint
```

4. ユニットテストの実行
// turbo
```powershell
npm test
```

5. テストが失敗した場合の自動原因調査
// turbo
```powershell
echo "テスト失敗を検知。エラーログを確認して原因を特定中..."
```

6. 最終状態の報告
// turbo
```powershell
echo "自動開発サイクルが完了したわ。詳細はチャットを確認してね！"
```
