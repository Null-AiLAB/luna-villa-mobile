---
description: 一括実行モード (複数コマンドの連続処理)
---

// turbo-all

1. 指定されたコマンドリストを取得する
// turbo
```powershell
echo "コマンドの一括実行を開始するわよ！"
```

2. 第1コマンドの実行
// turbo
```powershell
{{command1}}
```

3. 第2コマンドの実行 (あれば)
// turbo
```powershell
{{command2}}
```

4. 第3コマンドの実行 (あれば)
// turbo
```powershell
{{command3}}
```

5. 実行結果の確認
// turbo
```powershell
echo "すべてのコマンドの実行が終わったわ。エラーがないか確認してね♪"
```
