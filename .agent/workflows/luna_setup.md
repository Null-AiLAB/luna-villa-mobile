---
description: Project LUNA Setup (Turbo Mode)
---
// turbo-all

1. Create project directory `project_luna`
2. Create subdirectories (`src`, `data`, `prompts`)
3. Create definition files (`README.md`, `requirements.txt`)
